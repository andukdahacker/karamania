# Required for Story 1.1 (fail fast if missing)
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# Database (required when DB stories are implemented)
DATABASE_URL=postgresql://user:password@localhost:5432/karamania

# Firebase (required when auth/storage stories are implemented)
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_SERVICE_ACCOUNT_KEY=

# External APIs (required when song integration stories are implemented)
YOUTUBE_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
