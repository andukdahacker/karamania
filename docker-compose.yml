services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: karamania
      POSTGRES_PASSWORD: karamania
      POSTGRES_DB: karamania
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
